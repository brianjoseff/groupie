-form_for(:session, :url => sessions_path) do |f|
	%p.field
		=f.label :email
		=f.text_field :email
	
	%p.field
		=f.label :password
		=f.password_field :password
	
	%p.action
		=f.submit "Sign in"

