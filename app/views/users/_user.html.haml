%tr
	%td
		-if user.avatar?
			=link_to image_tag(user.avatar.url(:thumb), :alt => user.name), user_url(user)
		-else
			%p fuck
			%img{:src => ':rails_root/public/system/avatars/missing/thumb.png'}/
	%td
		=link_to user.name, user_url(user)
	%td
		%p=user.id